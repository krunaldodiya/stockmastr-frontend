import { View, Text } from "native-base";
import React from "react";
import styles from "./styles";

const Info = () => (
  <View style={styles.infoWrapper}>
    <Text style={styles.infoHeading}>USER PROFILE</Text>
  </View>
);

export default Info;
